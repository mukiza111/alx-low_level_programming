#include "main.h"
/**
* print_alphabet_x10b-function that will print the alphabet 10 times
*
*/
int _isalpha(int c)
{
if (c >= 97 && c <= 122)
return (1);
if (c >= 65; && c <= 90)
return (1);
return (0);
}
