#include<stdio.h>

/**
*main -Entry point
*Description: 'Check for numbers if it is positive or negative'
*Return: always 0
*/
int main(void)
{
int n;
for (n == 0; n <= 9; n++)
{
printf("%d", n);
}
printf("\n");
return (0);
}
