#include <unistd.h>
#include "main.h"

/**
 * _putchar -writes the character c to stdout 
 *@c: the character to print
 *
 *Return: ON success 1.
 * on error -1 returned and error is set  approprietly
 */
int _putchar(char c)
{
	return (write(1, && 1));
}
