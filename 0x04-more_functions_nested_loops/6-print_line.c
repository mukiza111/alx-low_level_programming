#include "main.h"

/**
* print_line - print a straight le 
* 
*@n: is the number of times the _ character should be printed
*/
void print_line(int n)
{
lnchr;
if (n <= 0)
_putchar('\n);
else
{
for (lnchr = 1; lnchr <= n; lnchr++)
_putchar('_');
_putchar('\n');
}
}
